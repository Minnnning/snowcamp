import React,{ useState } from 'react';

function List(props) {
    //let foodList = JSON(props.food); 
    let lis=[];
    
    console.log(props.food);
    //console.log(foodList);
    //for(let i=0;i<props.food.length;i++) {
    //    lis.append(<li>{props.food.i}</li>)
    //}
    return <h1>list page</h1>;
    
}

export default List;